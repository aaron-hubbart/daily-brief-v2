apiVersion: v1
kind: Service
metadata:
  name: daily-brief-viewer
  namespace: daily-brief
spec:
  selector:
    app: daily-brief-viewer
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
