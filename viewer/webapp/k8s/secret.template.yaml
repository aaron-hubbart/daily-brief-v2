# DO NOT commit real values. Use the kubectl command in k8s/README.md instead
# of applying this file directly. This is a reference for what keys exist.
apiVersion: v1
kind: Secret
metadata:
  name: daily-brief-secrets
  namespace: daily-brief
type: Opaque
stringData:
  FLASK_SECRET_KEY: "REPLACE-32-bytes-hex"
  AZURE_CLIENT_ID: "REPLACE"
  AZURE_CLIENT_SECRET: "REPLACE"
  AZURE_TENANT_ID: "REPLACE"
  UPLOAD_TOKENS: '{"REPLACE-token":"REPLACE-user-slug"}'
