apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: daily-brief-data
  namespace: daily-brief
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard-rwo
  resources:
    requests:
      storage: 1Gi
